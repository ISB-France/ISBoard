services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${DB_NAME:-isboard_recette}
      POSTGRES_USER: ${DB_USER:-isboard}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-isboard}
    ports:
      - "5435:5432"
    volumes:
      - pgdata-recette:/var/lib/postgresql/data
    restart: unless-stopped

  backend:
    build:
      context: ./app/backend
      dockerfile: Dockerfile.prod
    env_file: /etc/isboard/recette.env
    depends_on:
      db:
        condition: service_started
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: app/frontend/Dockerfile.prod
    ports:
      - "3005:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  pgdata-recette:
