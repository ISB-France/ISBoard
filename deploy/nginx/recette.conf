server {
    listen 80;
    server_name recette.monapp.com;

    # ── Protection par mot de passe ──
    auth_basic           "Espace Recette - Accès restreint";
    auth_basic_user_file /etc/nginx/.htpasswd-recette;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ── HTTPS (décommente quand le certificat SSL est prêt) ──
# server {
#     listen 443 ssl http2;
#     server_name recette.monapp.com;
#
#     ssl_certificate     /etc/letsencrypt/live/recette.monapp.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/recette.monapp.com/privkey.pem;
#
#     auth_basic           "Espace Recette - Accès restreint";
#     auth_basic_user_file /etc/nginx/.htpasswd-recette;
#
#     location / {
#         proxy_pass http://127.0.0.1:3001;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# ── Alternative : whitelist IP au lieu de mot de passe ──
# server {
#     listen 80;
#     server_name recette.monapp.com;
#
#     allow   192.168.1.0/24;
#     allow   <VOTRE_IP_PUBLIQUE>;
#     deny    all;
#
#     location / {
#         proxy_pass http://127.0.0.1:3001;
#         ...
#     }
# }
